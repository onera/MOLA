* reorganiser saveAll et ***saveSurfaces*** IMPORTANT !!

* changer interface noms des variables pour extractions de fields ou wall variables

* revoir Extractions interface (setup.py)
* EXTRACTIONS
  Include extractions for:
   -> probes
   -> slices (from existing variables, including distance to wall!)
   -> basic shapes (sphere, cube, cylinder)

OK * Coprocess:
OK     -> rm GridCoordinates_t avant moveCoordsFromEndOfRunToGridCoords
OK     -> moveCoordsFromEndOfRunToGridCoords: voir dependance PRE.addFieldExtractions(extractCoords=False)
NO * CO.moveCoordsFromEndOfRunToGridCoords : delete .Solver#Output

* Preprocess and eventually Coprocess:
  add :

  I._adaptZoneNamesForSlash(t)
  I._correctPyTree(t, level=10) # force CGNS names
  I._correctPyTree(t, level=2) # force unique name
  I._correctPyTree(t, level=7) # create familyNames

  in order to mimick corrections done when writing + reading a tree


* investigate iteration number tagging of MemoryUsageOfProc

OK * corriger tag AfterIter (-2 vers -1)

* (investiguer comment obtenir dernier loads)

* updateAndSaveLoads : concatener données de multiples IntegralData (différents tags)

OK  ignorer appels de fonctions si fields non trouvés (p.ex. MomentumXFlux non extraits)

OK * DOCUMENTATION (sphinx)

* TUTORIALS (sphinx)

* CO.boundaryConditions2Surfaces
  -> make it multi-container

* TEMPLATES -> correct versions of prepareMesh and prepareMainCGNS



* PREPROCESS
  -> Include computation of distance to wall
  -> include restart O(2)
  -> include special location of BC capabilities

* WORKFLOW AIRFOIL
  -> allow rounded trailing edge mesh generation

* NEW WORKFLOWS
  -> isolated propeller
  -> unsteady overset
  -> turbomachine
